




void main(){

    int a = Integer.parseInt(IO.readln("Entre com o primeiro numero: "));
    int b = Integer.parseInt(IO.readln("Entre com o segundo numero: "));

    IO.println("Soma: " + (a+b));

    if (a>b) {
        IO.println("O Maior numero é: " + a);
    } IO.println("O Maior numero é: 100" + b);

}